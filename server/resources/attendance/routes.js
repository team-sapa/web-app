var attendance = require('/attendance.js'), 
    attendance = require('../events/events.js'),
    member = require('../members/members.js'),
    express = require('express'), 
    router = express.Router();

//I DONT THINK WE NEED ANYTHING HERE
//ATTENDANCE DOCS ARE QUERIED FROM ROUTES HANDLED BY MEMBERS AND EVENTS

module.exports = router;